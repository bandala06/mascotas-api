spring.application.name=mascotas 

# Configuración dinámica para la base de datos
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/mascotas-data-base}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:m0405hb06}
spring.datasource.driver-class-name=org.postgresql.Driver


#mostrar las consultas SQL

spring.jpa.show-sql=true

#configuracion para que actualice el esquema
spring.jpa.hibernate.ddl-auto=update

#optimizar la base de datos
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

